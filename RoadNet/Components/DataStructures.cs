using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace VSIM_VEFR.RoadNet.Components
{
    public enum RoadOrientation { None, Vertical, Horizontal }
    public enum ComponentType { None, RoadSegment, Junction }
    public enum Direction { N, S, W, E }
    public enum TraficSignal { VerticalGreen, HorizontalGreen }

}
